# ── Bancos de dados (NUNCA versionar) ────────────────────────────────────────
Bancos/
*.FDB
*.fdb
*.GDB
*.gdb

# ── Segredos e configuração local ────────────────────────────────────────────
.env
*.env.local
agente.config.json
appsettings.*.local.json
appsettings.Production.json
.claude/settings.local.json
deploy/cf.json
# Segredos do Central/Railway (NUNCA versionar)
key-railway.txt
*.railway.txt
*-secrets.txt
seguranca.md
seguranca/

# ── Build .NET ────────────────────────────────────────────────────────────────
bin/
obj/
dist/
publish/
.tools/
*.user
*.suo
.vs/
*.nupkg

# ── Node / PWA ────────────────────────────────────────────────────────────────
node_modules/
npm-debug.log*

# ── Docker ────────────────────────────────────────────────────────────────────
nginx/certs/

# ── wwwroot gerado ────────────────────────────────────────────────────────────
backend/src/SigeDash.Api/wwwroot/

# ── Relatórios de segurança (não versionar o mapa de ataque) ──────────────────
security-reports/

# ── Assinatura de código (binários/cert locais; ficam em C:\CodeSign no servidor de build) ────
assinatura/

# ── OS ────────────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
